# reset the LoRa concentrator on packet-forwarder start
CONCENTRATOR_RESET="no"

# reset pin
CONCENTRATOR_RESET_PIN=25

# reset pin path
CONCENTRATOR_RESET_PIN_PATH=/sys/class/gpio/gpio$CONCENTRATOR_RESET_PIN
